import React from 'react';
import PropTypes from 'prop-types';
import { ScrollView, Text, View } from 'react-native';

import { styles } from './styles';

const Speaker = () => {
    return (
        <ScrollView>
            <Text>This is the Speaker view!</Text>
        </ScrollView>
    );
};

export default Speaker;

Speaker.propTypes = {

};


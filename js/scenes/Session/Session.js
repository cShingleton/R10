import React from 'react';
import PropTypes from 'prop-types';
import { ScrollView, Text, View } from 'react-native';

import { styles } from './styles';

const Session = () => {
    return (
        <ScrollView>
            <Text>This is the Session view!</Text>
        </ScrollView>
    );
};

export default Session;

Session.propTypes = {

};

